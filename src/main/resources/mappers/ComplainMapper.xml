<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.complanint.Repository.ComplainRepo">

    <select id="find" resultType="com.complanint.Entity.Complain">

        select * from complain where user_id=#{userId}
            order by created_at desc limit 1

    </select>



    <insert id="save" parameterType="com.complanint.Entity.Complain">
        insert into complain (user_id, title, content, category)
        values (#{userId}, #{title}, #{content}, #{category})

    </insert>


</mapper>